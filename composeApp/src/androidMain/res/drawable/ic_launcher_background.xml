<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Matches splash_bg (AppTheme.Ink900). Keeps the adaptive launcher
         icon on the same Ink900 surface as the cold-start splash + the
         OperationsHeader across the app — no white flash during the
         launcher → splash animation. -->
    <path
        android:fillColor="#FF0D1B3E"
        android:pathData="M0,0h108v108h-108z" />
</vector>
